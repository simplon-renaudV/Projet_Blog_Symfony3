{% extends "::base.html.twig" %}

{% block page %}

	<div class="flex-container">
		<div class="flex-item">
			{{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
<h1>UPDATE</h1>
	</div>

{% endblock %}
