/*jQuery(document).ready(function()
{
    num_article = $(this).attr('data-id');
    // On cache la zone de texte
    jQuery('.contenu').hide();
    // toggle() lorsque le lien avec l'ID #voir est cliqué
    jQuery(num_article).click(function()
    {
        jQuery('.contenu').toggle(800);
        return false;
    });
});*/